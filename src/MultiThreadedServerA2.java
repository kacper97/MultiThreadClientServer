
public class MultiThreadedServerA2 {

	
/* 
 * New thread created for client 
 * Validation of request compared to database
 * 
 * . Create a server hosted database (Assign2) and
 *  create the table myStudents using the structure
	below: (Populate this table with 4 records.)
• SID (type int(2), unique key)
• STUD_ID (type int(8))
• FNAME (type varchar(20) )
• SNAME (type varchar(20) )

Validate Communication by
displaying HostName and IPAddress
in the Client/Server windows with all messages sent
 */
}
